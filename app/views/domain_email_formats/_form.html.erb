<%= form_with(model: domain_email_format, local: true) do |form| %>
  <% if domain_email_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(domain_email_format.errors.count, "error") %> prohibited this domain_email_format from being saved:</h2>

      <ul>
      <% domain_email_format.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :format %>
    <%= form.text_field :format, id: :domain_email_format_format %>
  </div>

  <div class="field">
    <%= form.label :confirmed %>
    <%= form.text_field :confirmed, id: :domain_email_format_confirmed %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :domain_email_format_status %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, id: :domain_email_format_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
